apiVersion: v1
kind: Service
metadata:
  name: international-audit-streamlit
spec:
  type: ClusterIP
  externalTrafficPolicy: Local
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
  selector:
    app: international-audit-streamlit

