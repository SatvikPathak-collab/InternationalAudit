# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2
port: 80
image:
  repository: vidalprodacr.azurecr.io/FAA-International
  tag: latest

serviceName: faa-international-streamlit
releaseName: faa-international-streamlit

# KeyvaultName : insightrx-common
# KeyvaultName_common : insightrx-common

namespace: insightrx
label: faa-international-streamlit
TargetPort: 80
containerName: faa-international-streamlit
containerPort: 80

tolerations:  
- effect: NoSchedule
  key: appType
  operator: Equal
  value: mlprod

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
      - matchExpressions:
        - key: appType
          operator: In
          values:
          - mlprod
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
    - weight: 100
      podAffinityTerm:
        labelSelector:
          matchExpressions:
          - key: app
            operator: In
            values:
            -  faa-international-streamlit
        topologyKey: "kubernetes.io/hostname"
          

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 800m
    memory: 1000Mi
ServicePort: 80
name: faa-international-streamlit
podName: international-streamlit
environment: prod

# APP:
#   MONGO_DB: stp